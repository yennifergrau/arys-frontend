<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
  <!-- Debug only: allow user-installed CAs for MITM tools (Burp/ZAP). -->
  <base-config>
    <trust-anchors>
      <certificates src="system" />
      <certificates src="user" />
    </trust-anchors>
  </base-config>
</network-security-config>

